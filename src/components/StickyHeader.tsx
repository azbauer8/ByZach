import { twc } from "react-twc"

const StickyHeader = twc.div`sticky top-0 z-10 flex max-h-12 min-h-12 items-center justify-between border-b border-border px-3 py-2 backdrop-blur-lg`

export default StickyHeader
